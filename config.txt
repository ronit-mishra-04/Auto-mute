# Auto-Mute Configuration
# ============================================================================
# Add networks that should trigger speaker muting.
# You can use EITHER method (or both) for each network:
#
#   DNS domain:  Works reliably on macOS 26, no extra setup needed.
#                To find a WiFi's domain, connect to it and run:
#                  echo "show State:/Network/Global/DNS" | scutil
#                Look for "SearchDomains".
#
#   WiFi name:   Requires a macOS Shortcut called "Get-WiFi-Name".
#                Run ./setup_shortcut.sh for setup instructions.
#
# Format: One entry per line. Use DNS: or WIFI: prefix.
# DNS matches exact domains/subdomains; WiFi names match exactly.
# ============================================================================


# Seconds to wait after lid opens before checking network (default: 15)
# LID_OPEN_DELAY:15

# Examples (UNCOMMENT AND EDIT TO ADD MORE NETWORKS):
# DNS: <ADD YOUR WIFI DNS HERE>
# WIFI: <ADD YOUR WIFI NAME HERE>
WIFI:UIC-WiFi
WIFI:xROY 2GHz
WIFI:eduroam
